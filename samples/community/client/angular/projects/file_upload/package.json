{
  "name": "@a2ui/file_upload",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wireit",
    "format": "yarn --cwd ../.. format",
    "format:check": "yarn --cwd ../.. format:check",
    "lint": "yarn --cwd ../.. lint",
    "test": "yarn --cwd ../.. test",
    "lint:fix": "yarn --cwd ../.. lint:fix"
  },
  "dependencies": {
    "@a2ui/angular": "^0.10.0",
    "@a2ui/web_core": "^0.10.0",
    "ajv": "^8.20.0"
  },
  "devDependencies": {
    "wireit": "^0.15.0-pre.2"
  },
  "wireit": {
    "build": {
      "command": "yarn workspace angular-a2ui ng build file_upload",
      "files": [
        "src/**/*",
        "tsconfig.app.json"
      ],
      "output": [
        "dist"
      ]
    }
  }
}
